export default class Analyzer {
    /** Creates items from data (usually Markov class' learn() data) */
    toItems(data) {
        return []
    }

    /** Tokenizes items. */
    toTokens(item) {
        return []
    }
}
